dicionarios tienen clave y valor solo dos datos. pero mucha cantidad, estos pueden guardar datos en forma clave = (dato,nombre,edad,ubicacion,telefono,barrio, adoptado) y valor =(valor, damian, 25, bariloche, 2844333444, lera, no).
```python
diccionario={
    "dato":"valor",
    "nombre":"damian",
    "edad":"25",
    "ubicacion":"bariloche",
    "telefono":"2944333444",
    "barrio":"lera",
    "adoptado":"no",
     
}
```
lista: las listas consisten en elementos enumerados desde el 0 hacia adelante o desde el -1 hacia atras siendo el 0 el primer elemento y -1 el ultimo, con cualquier tipo de dato que puede ser modificado, cambiado, entre otros. Su forma es lista=[] en el van los elemntos con comillas.
```python
lista=["damian","24",33,"feliz","otro"]
```
tupla: son elementos sin modificaciones en el cual no se mueven ni se modifican en ningun sentido
```python
tupla=("damian", "cindy", "araceli", "eric")

```
conjunto. set(): no admite repetidos.
conjunto se establece asi conjunto = {} 
esto devuelve: numeros={1,2,3,4}
```python
lista_02=[1,2,2,3,4,4,4,4,4,4,4]
numeros=set(lista)
```

```python
```
```python
```
```python
```
```python
```
```python
```elif